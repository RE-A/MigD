<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.migd.mapper.PresetMapper">

    <resultMap id="presetResultMap" type="Preset">
        <id     property="id"          column="id"/>
        <result property="name"        column="name"/>
        <result property="description" column="description"/>
        <result property="createdAt"   column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="presetResultMap">
        SELECT * FROM PRESET ORDER BY created_at DESC
    </select>

    <select id="findById" parameterType="long" resultMap="presetResultMap">
        SELECT * FROM PRESET WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Preset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO PRESET (name, description)
        VALUES (#{name}, #{description})
    </insert>

    <update id="update" parameterType="Preset">
        UPDATE PRESET SET
            name        = #{name},
            description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM PRESET WHERE id = #{id}
    </delete>

</mapper>
